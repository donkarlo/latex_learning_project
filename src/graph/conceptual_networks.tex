\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{graphs,graphdrawing,arrows.meta}
\usegdlibrary{force} % spring layout
\begin{document}
    from pycharm on the top it the run configuration to run with lualatex
    \begin{tikzpicture}[>=Stealth, nodes={draw,rounded corners,inner sep=3pt}]
        \graph [spring layout, node distance=8mm] {
                { "Self-awareness", "Novelty detection", "MRS", "Temporal models" }
            -- { "DBN", "LSTM" };
            "Self-awareness" -- { "Proprioceptive", "Exteroceptive" };
            "MRS" -- { "Coupled models", "Coordination" };
            "Novelty detection" -- { "Hausdorff", "KL-divergence" };
        };
    \end{tikzpicture}
\end{document}